---
phase: 02-desktop-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Calculator.tsx
autonomous: false

must_haves:
  truths:
    - "Desktop layout uses two columns with inputs left, results right"
    - "Layout feels spacious with generous whitespace on wide screens"
    - "Results panel is sticky and visible while scrolling inputs"
    - "Visual hierarchy clearly distinguishes primary results from secondary info"
    - "Column gap and internal spacing are noticeably larger than mobile"
  artifacts:
    - path: "src/components/Calculator.tsx"
      provides: "Desktop-polished two-column layout"
      contains: "lg:grid-cols-[1fr,400px]"
    - path: "src/components/Calculator.tsx"
      provides: "Increased spacing scale"
      contains: "gap-6 lg:gap-8"
  key_links:
    - from: "Calculator.tsx grid container"
      to: "lg: breakpoint classes"
      via: "Tailwind responsive prefixes"
      pattern: "lg:grid-cols|lg:gap|lg:space-y"
    - from: "Results column"
      to: "Sticky positioning"
      via: "lg:sticky lg:top-4 lg:self-start"
      pattern: "lg:sticky.*lg:self-start"
---

<objective>
Polish the desktop layout to feel spacious, professional, and easy to read.

Purpose: Transform the calculator from functional to delightful on desktop screens. Users should feel the difference when using on a laptop or desktop monitor - generous whitespace, clear visual hierarchy, and a premium feel that makes the tool pleasant to use and share.

Output: Updated Calculator.tsx with enhanced grid layout, spacing, and visual hierarchy optimized for desktop screens (768px+).
</objective>

<execution_context>
@/Users/applebya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/applebya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-desktop-polish/02-CONTEXT.md
@.planning/phases/02-desktop-polish/02-RESEARCH.md
@src/components/Calculator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance grid layout and column structure</name>
  <files>src/components/Calculator.tsx</files>
  <action>
Update the main grid container (line 278) with these changes:

1. **Grid columns**: Change `lg:grid-cols-[1fr,380px]` to `lg:grid-cols-[1fr,400px]`
   - Wider results column (400px vs 380px) provides breathing room for dual-currency displays
   - Keep `grid-cols-1` for mobile stacking

2. **Grid gap**: Change `gap-4 lg:gap-6` to `gap-6 lg:gap-8`
   - More generous spacing between columns on desktop (32px vs 24px)
   - Mobile gets 24px (was 16px)

3. **Remove grid-rows**: Remove `lg:grid-rows-[auto,1fr]`
   - Unnecessary with sticky positioning
   - Simplifies grid structure

4. **Results column sticky**: Ensure results column (around line 959) has:
   - `lg:sticky lg:top-4 lg:self-start`
   - This keeps results visible while scrolling through settings
   - `lg:self-start` prevents stretching to full grid row height

The updated grid container should look like:

```tsx
<div className="grid grid-cols-1 lg:grid-cols-[1fr,400px] gap-6 lg:gap-8">
```

And results column wrapper:

```tsx
<div className="lg:sticky lg:top-4 lg:self-start space-y-3 order-1 lg:order-3 lg:row-span-2">
```

  </action>
  <verify>
Run `bun run build` - should pass with no TypeScript errors.
Visually check at 1440px width that columns have more space between them.
  </verify>
  <done>Grid uses 400px results column, 32px gap on desktop, sticky results panel stays visible during scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Polish spacing, whitespace, and visual hierarchy</name>
  <files>src/components/Calculator.tsx</files>
  <action>
Enhance internal spacing and visual hierarchy throughout the component:

**1. Left column spacing (inputs section, around line 280):**

- Change `space-y-3` to `space-y-4 lg:space-y-6`
- More breathing room between employment type selector and rate input

**2. Settings panel container (around line 497):**

- Increase gap between expand button and settings panel
- Change `mb-1` to `mb-2 lg:mb-3`

**3. Settings collapsible sections (Currency, Schedule, Tax - lines 513, 650, 836):**

- Keep current padding `px-3 py-2.5` but add responsive variant `lg:px-4 lg:py-3`
- Increase CollapsibleContent padding: `px-3 pt-3 pb-3` to `px-3 pt-3 pb-3 lg:px-4 lg:pt-4 lg:pb-4`
- Increase internal `space-y-3` to `space-y-3 lg:space-y-4` within CollapsibleContent

**4. Results card (around line 961):**

- Increase CardContent padding: `p-3 sm:p-4` to `p-4 sm:p-5 lg:p-6`
- Increase space-y in CardContent: `space-y-3 sm:space-y-4` to `space-y-4 lg:space-y-5`

**5. Typography refinement in results:**

- Hero numbers: Keep `text-3xl sm:text-4xl` (already good)
- Period breakdown labels: Keep `text-xs sm:text-sm` (already good)
- Add `lg:text-base` to period breakdown values for slightly larger text on desktop

**6. Bottom spacing:**

- Results card wrapper `space-y-3` to `space-y-3 lg:space-y-4`
- Share button and exchange rate display get more room

Key principle: Use responsive suffixes (`lg:`) to add MORE space on desktop without changing mobile. Desktop screens have room to breathe; mobile needs density.
</action>
<verify>
Run `bun run build` - should pass with no TypeScript errors.
Visually compare mobile (375px) vs desktop (1440px) - desktop should have noticeably more whitespace.
</verify>
<done>Desktop layout has generous whitespace with 8px grid spacing system. Settings panels, results card, and all sections have increased padding on lg: breakpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of desktop layout</name>
  <what-built>
Desktop-polished two-column layout with:
- Wider results column (400px)
- Increased gaps between columns (32px on desktop)
- More generous padding inside all sections
- Sticky results panel
- Enhanced visual hierarchy
  </what-built>
  <how-to-verify>
1. Run `bun run dev` to start dev server
2. Open http://localhost:5173 in browser
3. Resize to desktop width (1440px or wider)
4. Verify:
   - [ ] Two-column layout: inputs left, results right
   - [ ] Results panel stays visible when scrolling settings
   - [ ] Generous whitespace between and within sections
   - [ ] Visual hierarchy: hero numbers stand out, labels subdued
   - [ ] No cramped feeling - layout uses horizontal space well
5. Resize to mobile (375px) and verify:
   - [ ] Still looks good on mobile (no excessive spacing)
   - [ ] Stacked layout with results on top
6. Try intermediate sizes (768px, 1024px) - transitions should feel natural
  </how-to-verify>
  <resume-signal>Type "approved" if layout meets requirements, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `bun run build` passes with no TypeScript errors
- `bun run test:run` passes all tests
- Visual inspection at 1440px shows spacious two-column layout
- Visual inspection at 375px shows clean mobile layout (no regression)
- Results panel sticky on desktop (scrolls with viewport, not page)
</verification>

<success_criteria>

1. Desktop (768px+) uses two-column layout with inputs left, results right
2. Results column is 400px wide with sticky positioning
3. Gap between columns is 32px on desktop (was 24px)
4. All sections have increased padding on desktop via lg: prefixes
5. Visual hierarchy is clear: hero numbers prominent, labels subdued
6. Mobile layout unchanged or improved (no regression)
7. Build passes, all tests pass
   </success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-polish/02-01-SUMMARY.md`
</output>
