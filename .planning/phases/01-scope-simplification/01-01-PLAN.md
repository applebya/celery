---
phase: 01-scope-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/hooks/useCalculation.ts
  - src/data/tax-brackets-2026.ts
  - src/components/Calculator.tsx
autonomous: true

must_haves:
  truths:
    - "Corporation employment type is not visible in UI"
    - "Calculator works correctly for contractor and employee types"
    - "No TypeScript errors in build"
    - "No console errors when using calculator"
  artifacts:
    - path: "src/types.ts"
      provides: "CalculatorState without corp fields"
      must_not_contain: "isIncorporated"
    - path: "src/hooks/useCalculation.ts"
      provides: "Calculation hook without corp breakdown"
      must_not_contain: "CorpBreakdown"
    - path: "src/data/tax-brackets-2026.ts"
      provides: "Tax data without corp rates"
      must_not_contain: "canadaCorpTaxRates"
    - path: "src/components/Calculator.tsx"
      provides: "Calculator UI without corp settings"
      must_not_contain: "Building2"
  key_links:
    - from: "src/components/Calculator.tsx"
      to: "src/hooks/useCalculation.ts"
      via: "useCalculation hook"
      pattern: "useCalculation\\(state\\)"
    - from: "src/hooks/useCalculation.ts"
      to: "src/types.ts"
      via: "CalculatorState type"
      pattern: "CalculatorState"
---

<objective>
Remove corporation employment type from Celery calculator to simplify scope.

Purpose: The corporation feature (CCPC for Canada, S-Corp for US) adds complexity without serving the core use case. Removing it simplifies the UI and reduces maintenance burden.

Output: Calculator with only contractor (hourly/retainer) and employee (hourly/salary) types. All corp-related code removed cleanly.
</objective>

<execution_context>
@/Users/applebya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/applebya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scope-simplification/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove corp types, calculation logic, and tax data</name>
  <files>
    src/types.ts
    src/hooks/useCalculation.ts
    src/data/tax-brackets-2026.ts
  </files>
  <action>
Remove corporation-related code from types and calculation layer:

**src/types.ts:**

1. Remove these 5 fields from CalculatorState interface:
   - `isIncorporated: boolean`
   - `corpRetentionPercent: number`
   - `dividendVsSalaryPercent: number`
   - `corpTaxRateOverride?: number`
   - `dividendTaxRateOverride?: number`
2. Remove same 5 fields from DEFAULT_STATE object

**src/hooks/useCalculation.ts:**

1. Remove the `CorpBreakdown` interface entirely (lines 8-35)
2. Remove `corpBreakdown` field from `CalculationResult` interface
3. Remove import of `getCorpTaxRate`, `getDividendTaxRate`, `calculateSEtaxSavings` from tax-brackets-2026
4. Remove `calculateCorpBreakdown` function entirely (lines 149-234)
5. Remove corp-related logic from the useMemo:
   - Remove lines 104-109 (if isIncorporated block)
   - Remove `corpBreakdown` from return object
6. Remove corp-related dependencies from useMemo array:
   - `state.isIncorporated`
   - `state.corpRetentionPercent`
   - `state.dividendVsSalaryPercent`
   - `state.corpTaxRateOverride`
   - `state.dividendTaxRateOverride`

**src/data/tax-brackets-2026.ts:**

1. Remove `canadaCorpTaxRates` object (lines 240-254)
2. Remove `canadaDividendTaxRates` object (lines 262-276)
3. Remove `usSCorpConfig` object (lines 284-295)
4. Remove `getCorpTaxRate` function (lines 300-306)
5. Remove `getDividendTaxRate` function (lines 311-318)
6. Remove `calculateSEtaxSavings` function (lines 323-336)
7. Remove the section comment "CORPORATION TAX RATES" (lines 231-233)
   </action>
   <verify>
   Run `bun run build` - should complete with no TypeScript errors.
   </verify>
   <done>
   All corp-related types, interfaces, functions, and tax data removed. Build passes.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Remove corp UI components from Calculator</name>
  <files>
    src/components/Calculator.tsx
  </files>
  <action>
Remove corporation-related UI from Calculator component:

1. **Remove imports:**
   - Remove `Building2` from lucide-react import
   - Remove `getCorpTaxRate`, `getDividendTaxRate` from tax-brackets-2026 import

2. **Update handleEmploymentTypeChange callback:**
   - Remove the line: `isIncorporated: isContractor ? state.isIncorporated : false,`
   - Remove `state.isIncorporated` from the dependency array

3. **Remove Corporation Settings collapsible section:**
   - Remove the entire Collapsible block for corporation settings (lines 655-860)
   - This is the section with `openSection === 'corp'` and the Building2 icon

4. **Remove corporation breakdown results display:**
   - Remove the entire conditional block: `{calculation.corpBreakdown && state.showTaxEstimate && (...)}`
   - This is approximately lines 996-1100 that render Total Net Worth, Personal Section, Corporation Section, and S-Corp Savings

5. **Simplify results conditional:**
   - Change `{(!calculation.corpBreakdown || !state.showTaxEstimate) && state.calculationMode === 'hourlyToSalary' ? (`
   - To simply: `{state.calculationMode === 'hourlyToSalary' ? (`

Note: Keep `isContractorType` import - it's still used for retainer mode display logic.
</action>
<verify>

1. Run `bun run build` - no TypeScript errors
2. Run `bun run dev` and verify:
   - No "Corporation" section in settings panel
   - Employment type selector shows: Contractor (hourly), Contractor (retainer), Employee (hourly), Employee (salary)
   - Results display correctly for all 4 employment types
   - No console errors
     </verify>
     <done>
     Calculator UI has no corporation settings section. All 4 employment types work. No console errors.
     </done>
     </task>

</tasks>

<verification>
1. `bun run build` passes with no TypeScript errors
2. `bun run test:run` - all existing tests pass (may need to update if corp tests exist)
3. Manual testing:
   - Select each employment type: contractor-hourly, contractor-retainer, employee-hourly, employee-salary
   - Enter values and verify calculations display correctly
   - Toggle tax estimate on/off
   - No "Corporation" or "Incorporated" text visible anywhere
   - No console errors during interaction
</verification>

<success_criteria>

1. Corporation employment type removed from UI dropdown
2. All references to corporation type removed from labels and help text
3. Existing calculations work correctly for employee and self-employed types
4. No console errors or broken UI from removal
5. Build passes with no TypeScript errors
   </success_criteria>

<output>
After completion, create `.planning/phases/01-scope-simplification/01-01-SUMMARY.md`
</output>
